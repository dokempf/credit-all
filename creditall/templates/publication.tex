Contributors:
\begin{itemize}
{% for c in contributors -%}
  \item {{ c.name }} ({% for t in c.contributions %}{{ types[t]['description'] }}{{ ", " if not loop.last }}{% endfor %})
{% endfor -%}
\end{itemize}
